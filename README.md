# Library-System
build a Library Book Management System that can: Add books to the library (title, author, genre). Borrow books, updating a popularity score for each book. Return books (optional bonus: track borrowing history). Show the current list of books and their popularity scores. Save the data in a JSON file, so it persists when the program closes
